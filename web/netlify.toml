# Used when the Netlify site's base directory is set to `web`.
# The repo-root netlify.toml already points base at this folder.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
